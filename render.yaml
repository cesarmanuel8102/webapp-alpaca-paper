services:
  - type: web
    name: webapp-alpaca-paper-oauth
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: FLASK_SECRET_KEY
        value: test_secret
