services:
  - type: web
    name: webapp-alpaca
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
